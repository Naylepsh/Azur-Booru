<%- include('../partials/header', { stylesheets: [
  "https://fonts.googleapis.com/css?family=Lato:400,700&display=swap",
  "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css",
  "/css/main.css",
  "/css/posts.css"
], title: post.id
}) %>
<body>
  <% include ../partials/nav-bar.ejs %>
  <div class="container">
    <% include ../partials/search-bar.ejs %>
    <aside class="sidebar">
      <% include ../partials/tag-list.ejs %>
      <section class="sidebar-section-generic">
        <div class="sidebar-section-name">
          <h3>Information</h3>
        </div>
        <ul>
          <div><li class="sidebar-section-generic-content"><span>Id:</span><span><%= post.id %></span></li></div>
          <div><li class="sidebar-section-generic-content"><span>Source:</span><span><%= post.source %></span></li></div>
        </ul>
      </section>
      <section class="sidebar-section-generic">
        <div class="sidebar-section-name">
          <h3>Options</h3> 
        </div>
        <ul>
          <div><li class="sidebar-section-generic-content"><a href="/posts/<%= post.id %>/edit">Edit</a></li></div>
          <% if (user && user._id === post.author._id.toString()) { %>
          <div>
            <li class="sidebar-section-generic-content">
              <form action="/posts/<%= post.id %>?_method=DELETE" method="POST">
                <button id="delete-button">Delete</button>
              </form>
            </li>
          </div>
          <% } %>
        </ul>
      </section>
    </aside>
    <div id="content">
      <section class="post-image">
        <img src='<%= post.imageLink %>'>
      </section>
      <section class="post-comments">
        <% if (post.comments && post.comments.length > 0) { %>
          <div class="comments-list">
            <ul>
              <% for (const comment of post.comments) { %>
                <li class="comment">
                  <div class="comment-author-avatar">
                    <img src="/assets/default-avatar.jpg" alt="<%= comment.author.name %> avatar">
                  </div>
                  <div class="comment-author-info">
                    <a href="#"><%= comment.author.name %></a>
                  </div>
                  <div class="comment-body">
                    <%= comment.body %>
                  </div>
                </li>
              <% } %>
            </ul>
          </div>
        <% } %>
        <div class="comment-new">
          <form action="/posts/<%= post._id %>/comments" method="POST">
            <div class="form-entry">
              <label for="body">Body</label>
              <textarea name="comment[body]" id="body"></textarea>
            </div>
            <button>Submit</button>
          </form>
        </div>
      </section>
    </div>
  </div>

  <script src="/js/tags.js"></script>
  <script src="/js/sidebar.js"></script>
<% include ../partials/footer.ejs %>